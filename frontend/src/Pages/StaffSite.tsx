import React from "react";

export const StaffSite: React.FC = () => {
  return <div>staff site</div>;
};

// <Header />
// <NavBar />
// if dashboard && <StaffDashboard />
// if delivery && <StoreDelivery />
// if stocklevel && <StoreStockLevel />
// if reports && <Reports /> * stretch goal